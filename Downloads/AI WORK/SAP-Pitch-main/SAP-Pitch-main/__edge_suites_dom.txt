<!DOCTYPE html>
<html lang="en" style="scroll-behavior: smooth;"><head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>SAP Suites | Baylor x SAP</title>
  <link rel="icon" type="image/png" href="favicon.png">
  <link rel="shortcut icon" href="favicon.ico">
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin="">
  <link href="https://fonts.googleapis.com/css2?family=Space+Grotesk:wght@400;500;700&amp;display=swap" rel="stylesheet">
  <style>
    :root {
      --bg: #16392f;
      --bg-soft: #1d483c;
      --bg-deep: #0c221c;
      --surface: rgba(25, 61, 50, 0.8);
      --surface-strong: rgba(19, 48, 39, 0.9);
      --text: rgba(247, 242, 232, 0.94);
      --muted: rgba(228, 219, 202, 0.74);
      --line: rgba(231, 216, 182, 0.16);
      --line-strong: rgba(182, 139, 47, 0.48);
      --gold: #b68b2f;
      --gold-soft: #dcc89a;
      --blue: #245647;
      --shadow-panel: 0 24px 72px rgba(6, 20, 16, 0.28);
    }

    * {
      box-sizing: border-box;
    }

    html {
      scroll-behavior: smooth;
    }

    body {
      margin: 0;
      background-color: var(--bg-deep);
      background-image:
        radial-gradient(circle at top left, rgba(182, 139, 47, 0.18), transparent 30%),
        radial-gradient(circle at 85% 15%, rgba(34, 84, 70, 0.32), transparent 34%),
        radial-gradient(circle at bottom center, rgba(20, 56, 46, 0.5), transparent 42%);
      color: var(--text);
      font-family: "Space Grotesk", sans-serif;
      overflow-x: hidden;
    }

    a {
      color: inherit;
      text-decoration: none;
    }

    img {
      display: block;
      max-width: 100%;
    }

    .page-transition {
      position: fixed;
      inset: 0;
      z-index: 9999;
      pointer-events: none;
      overflow: hidden;
    }

    .page-transition__panel {
      position: absolute;
      top: 50%;
      left: 50%;
      width: min(34vw, 460px);
      height: min(22vw, 300px);
      background: #16392f;
      border-radius: 28px;
      transform: translate(-50%, -50%) scale(0.88);
      transform-origin: center center;
      opacity: 0;
      will-change: transform, opacity, border-radius, width, height;
      backface-visibility: hidden;
      box-shadow: 0 32px 96px rgba(5, 18, 14, 0.28);
    }

    .page-transition.is-leaving {
      pointer-events: all;
    }

    body.is-transitioning {
      overflow: hidden;
    }

    .hero {
      position: relative;
      min-height: 100vh;
      overflow: hidden;
      background: transparent;
    }

    .hero__media,
    .hero__shade,
    .hero__grid {
      position: absolute;
      inset: 0;
    }

    .hero__media img {
      width: 100%;
      height: 100%;
      object-fit: cover;
      transform: scale(1.15);
      filter: saturate(0.88) contrast(1.02);
    }

    .hero__shade {
      background:
        radial-gradient(circle at 20% 20%, rgba(182, 139, 47, 0.18), transparent 32%),
        rgba(12, 34, 28, 0.62);
    }

    .hero__grid {
      background-image:
        linear-gradient(rgba(255, 255, 255, 0.04) 1px, transparent 1px),
        linear-gradient(90deg, rgba(255, 255, 255, 0.04) 1px, transparent 1px);
      background-size: 72px 72px;
      opacity: 0.44;
    }

    .hero__inner,
    .section__inner {
      position: relative;
      z-index: 2;
      width: min(1320px, calc(100% - 72px));
      margin: 0 auto;
    }

    .hero__inner {
      min-height: 100vh;
      display: grid;
      grid-template-columns: minmax(0, 1fr) minmax(320px, 400px);
      gap: clamp(28px, 5vw, 72px);
      align-items: end;
      padding: 12vh 0 18vh;
    }

    .hero__copy {
      max-width: 760px;
      padding-left: 18px;
      border-left: 1px solid rgba(220, 200, 154, 0.42);
    }

    .eyebrow {
      margin: 0 0 14px;
      font-size: 11px;
      font-weight: 600;
      letter-spacing: 0.1em;
      text-transform: uppercase;
      color: rgba(220, 200, 154, 0.84);
    }

    .title {
      margin: 0;
      font-size: clamp(3.4rem, 8vw, 6.6rem);
      line-height: 0.92;
      letter-spacing: -0.03em;
      font-weight: 700;
      text-wrap: balance;
    }

    .title span {
      color: var(--gold);
    }

    .lead {
      margin: 22px 0 0;
      max-width: 700px;
      font-size: clamp(1.05rem, 1.85vw, 1.42rem);
      line-height: 1.72;
      color: var(--muted);
    }

    .hero__actions {
      display: flex;
      flex-wrap: wrap;
      gap: 12px;
      margin-top: 28px;
    }

    .button {
      display: inline-flex;
      align-items: center;
      justify-content: center;
      min-height: 48px;
      padding: 0 16px;
      border: 1px solid var(--line);
      border-radius: 999px;
      background: rgba(247, 242, 232, 0.05);
      color: var(--text);
      font-size: 12px;
      font-weight: 600;
      letter-spacing: 0.1em;
      text-transform: uppercase;
      transition: border-color 0.2s ease, color 0.2s ease, background-color 0.2s ease, transform 0.2s ease;
    }

    .button:hover {
      border-color: var(--gold-soft);
      background: rgba(247, 242, 232, 0.1);
      color: #fff;
      transform: translateY(-1px);
    }

    .button--primary {
      border-color: rgba(220, 200, 154, 0.9);
      background: rgba(182, 139, 47, 0.95);
      color: #132b24;
    }

    .button--primary:hover {
      background: #c49a3d;
      color: #0c1d18;
    }

    .terminal {
      width: 100%;
      padding: 24px;
      border: 1px solid var(--line);
      border-radius: 28px;
      background: var(--surface-strong);
      box-shadow: var(--shadow-panel);
      backdrop-filter: blur(18px);
      align-self: end;
    }

    .terminal__label,
    .metric__label {
      margin: 0;
      font-size: 10px;
      font-weight: 600;
      letter-spacing: 0.1em;
      text-transform: uppercase;
      color: rgba(220, 200, 154, 0.74);
    }

    .terminal__title {
      margin: 14px 0 0;
      font-size: clamp(1.8rem, 3vw, 2.5rem);
      line-height: 0.98;
      letter-spacing: -0.02em;
      font-weight: 700;
      max-width: 10ch;
    }

    .terminal__copy {
      margin: 18px 0 0;
      font-size: 14px;
      line-height: 1.78;
      color: var(--muted);
    }

    .terminal__meta {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 12px;
      margin-top: 22px;
      padding-top: 16px;
      border-top: 1px solid rgba(231, 216, 182, 0.12);
    }

    .metric strong {
      display: block;
      margin-top: 6px;
      font-size: 13px;
      line-height: 1.65;
      color: rgba(247, 242, 232, 0.96);
    }

    .terminal__link {
      display: inline-flex;
      align-items: center;
      gap: 10px;
      width: 100%;
      margin-top: 20px;
      padding-top: 14px;
      border-top: 1px solid rgba(231, 216, 182, 0.12);
      font-size: 12px;
      font-weight: 600;
      letter-spacing: 0.1em;
      text-transform: uppercase;
    }

    .terminal__link::after {
      content: "";
      flex: 1;
      height: 1px;
      background: linear-gradient(90deg, rgba(182, 139, 47, 0.7), rgba(220, 200, 154, 0.2));
    }

    .terminal__link:hover {
      color: var(--gold);
    }

    .section {
      position: relative;
      padding: 36px 0;
      background: transparent;
      overflow: hidden;
    }

    .grid {
      display: grid;
      gap: 22px;
    }

    .grid--2 {
      grid-template-columns: repeat(2, minmax(0, 1fr));
    }

    .grid--3 {
      grid-template-columns: repeat(3, minmax(0, 1fr));
    }

    .panel,
    .visual {
      border: 1px solid var(--line);
      border-radius: 28px;
      background: var(--surface-strong);
      box-shadow: var(--shadow-panel);
      backdrop-filter: blur(18px);
    }

    .panel {
      padding: 24px;
    }

    .panel h2,
    .panel h3 {
      margin: 0;
      font-size: clamp(1.6rem, 3vw, 2.4rem);
      line-height: 1.02;
      letter-spacing: -0.02em;
      font-weight: 700;
    }

    .panel p,
    .panel li {
      font-size: 14px;
      line-height: 1.78;
      color: var(--muted);
    }

    .panel p + p {
      margin-top: 10px;
    }

    .list {
      margin: 18px 0 0;
      padding-left: 18px;
    }

    .list li + li {
      margin-top: 10px;
    }

    .note {
      margin: 16px 0 0;
      font-size: 13px;
      line-height: 1.7;
      color: rgba(255, 255, 255, 0.54);
    }

    .visual {
      display: grid;
      grid-template-rows: auto 1fr;
      overflow: hidden;
    }

    .visual img {
      width: 100%;
      aspect-ratio: 4 / 3;
      object-fit: cover;
      transform: scale(1.15);
      position: relative;
      z-index: 1;
    }

    .visual__meta {
      position: relative;
      z-index: 2;
      padding: 16px 18px 18px;
      background: rgba(17, 44, 36, 0.96);
      border-top: 1px solid rgba(220, 200, 154, 0.2);
    }

    .visual__meta strong {
      display: block;
      margin-bottom: 6px;
      font-size: 1rem;
      font-weight: 700;
    }

    .visual__meta span {
      font-size: 14px;
      line-height: 1.72;
      color: var(--muted);
    }

    .nav-island {
      position: fixed;
      bottom: 40px;
      left: 50%;
      transform: translateX(-50%);
      display: flex;
      gap: 2rem;
      background: rgba(18, 47, 39, 0.84);
      backdrop-filter: blur(12px);
      -webkit-backdrop-filter: blur(12px);
      padding: 1rem 2.5rem;
      border-radius: 100px;
      border: 1px solid var(--line);
      box-shadow: 0 18px 44px rgba(8, 22, 18, 0.24);
      pointer-events: auto;
      z-index: 100;
    }

    .nav-island a {
      font-size: 13px;
      font-weight: 600;
      letter-spacing: 0.08em;
      text-transform: uppercase;
      color: rgba(247, 242, 232, 0.9);
      opacity: 0.72;
      transition: opacity 0.25s ease, color 0.25s ease;
      white-space: nowrap;
    }

    .nav-island a:hover,
    .nav-island a.is-active {
      opacity: 1;
      color: var(--gold);
    }

    @media (max-width: 1100px) {
      .hero__inner {
        grid-template-columns: 1fr;
      }

      .grid--3 {
        grid-template-columns: 1fr;
      }
    }

    @media (max-width: 820px) {
      .hero__inner,
      .section__inner {
        width: calc(100% - 32px);
      }

      .grid--2 {
        grid-template-columns: 1fr;
      }

      .title {
        font-size: clamp(3rem, 14vw, 5.6rem);
      }

      .lead {
        font-size: 1.02rem;
      }

      .terminal__meta {
        grid-template-columns: 1fr;
      }
    }

    @media (max-width: 640px) {
      .nav-island {
        width: calc(100% - 24px);
        gap: 1rem;
        justify-content: space-between;
        padding: 0.9rem 1rem;
        bottom: 22px;
      }

      .nav-island a {
        font-size: 10px;
      }

      .hero__inner,
      .section__inner {
        width: calc(100% - 24px);
      }
    }
  </style>
</head>
<body style="">
  <div class="page-transition" aria-hidden="true">
    <div class="page-transition__panel" style="opacity: 0; left: 50%; top: 50%; width: min(34vw, 460px); height: min(22vw, 300px); border-radius: 28px; transform: translate(-50%, -50%) scale(0.88);"></div>
  </div>

  <main>
    <section class="hero">
      <div class="hero__media">
        <img src="sap-suite-table.png" alt="SAP-powered iPad Pro suite experience" style="translate: none; rotate: none; scale: none; transform: scale(1.15, 1.15);">
      </div>
      <div class="hero__shade"></div>
      <div class="hero__grid"></div>

      <div class="hero__inner">
        <div class="hero__copy">
          <p class="eyebrow">Activation 01 · Suites</p>
          <h1 class="title">Executive hospitality with visible <span>product proof</span></h1>
          <p class="lead">SAP is the hero here: the company wants credible regional business access, but the obstacle is that enterprise software can feel distant inside a sports environment. Baylor solves that by turning premium suites into a live product-proof room while also integrating SAP technology throughout the stadium so the suite feels like the premium front row to a larger connected experience. The suite itself adds prestige, which matters because a truly premium environment changes how guests feel about the partner hosting it and raises SAP's perceived authority in the room.</p>
          <div class="hero__actions">
            <a class="button button--primary" href="contact.html">Start Partnership Discussion</a>
            <a class="button" href="packages.html">See The App Tie-In</a>
          </div>
        </div>

        <aside class="terminal">
          <p class="terminal__label">Executive Engagement Strategy</p>
          <h2 class="terminal__title">Utility first. Branding second.</h2>
          <p class="terminal__copy">The suite level is where Baylor proves SAP can host the right people, run a disciplined demonstration, and turn a game day room into a controlled business environment while the broader stadium also reflects SAP-powered data, wayfinding, and game intelligence touchpoints. A super-premium room adds status, and that prestige helps SAP feel like the kind of partner serious businesses want to be aligned with.</p>
          <div class="terminal__meta">
            <div class="metric">
              <p class="metric__label">Primary value</p>
              <strong>Client hospitality + demos</strong>
            </div>
            <div class="metric">
              <p class="metric__label">Rubric fit</p>
              <strong>B2B crossover and education</strong>
            </div>
          </div>
          <a class="terminal__link" href="sap-keynote.html">Continue To Keynotes</a>
        </aside>
      </div>
    </section>

    <section class="section">
      <div class="section__inner">
        <div class="grid grid--2">
          <article class="panel">
            <p class="eyebrow">Guest journey</p>
            <h2>What the suite experience should actually do</h2>
            <ul class="list">
              <li>Welcome guests into an SAP-branded room that feels current, minimal, and enterprise-grade.</li>
              <li>Use the central table to surface live player data, game flow, and product demonstrations in one shared view.</li>
              <li>Extend the experience with per-seat iPad Pro integrations so every guest sees a personalized analytics layer during the game.</li>
              <li>End with a host-led walkthrough that connects visible stadium data to SAP's real business capabilities.</li>
            </ul>
          </article>

          <article class="panel">
            <p class="eyebrow">Why it matters</p>
            <h2>This is where the partnership starts to look enterprise.</h2>
            <p>The suite environment gives Baylor something most college sponsorship pitches lack: a place where SAP can host the right people while visibly demonstrating how its technology works.</p>
            <p>Instead of a logo in the background, SAP becomes the intelligence layer of the room and a visible technology partner throughout the stadium environment.</p>
            <p>The prestige of a truly high-end suite also creates its own value: when guests experience a room that feels exclusive, polished, and well-run, that quality reflects back on SAP as a premium partner worth taking seriously.</p>
            <p class="note">This page is designed to support the rubric's B2B crossover, relationship building, and education requirements in one place.</p>
          </article>
        </div>
      </div>
    </section>

    <section class="section">
      <div class="section__inner">
        <div class="grid grid--3">
          <article class="visual">
            <img src="sap-suite-table.png" alt="Center table analytics experience" style="translate: none; rotate: none; scale: none; transform: translate(0%, -35%) scale(1.15, 1.15);">
            <div class="visual__meta">
              <strong>Center table</strong>
              <span>Shared live dashboard for hospitality groups, hosted demonstrations, and co-viewing.</span>
            </div>
          </article>

          <article class="visual">
            <img src="sap-suite-row.png" alt="Per-seat analytics surfaces" style="translate: none; rotate: none; scale: none; transform: translate(0%, -35%) scale(1.15, 1.15);">
            <div class="visual__meta">
              <strong>Per-seat analytics</strong>
              <span>Seat-by-seat dashboards keep every guest engaged with real-time data and contextual prompts.</span>
            </div>
          </article>

          <article class="visual">
            <img src="sap-suite-analytics.png" alt="Hosted analytics demonstration layer" style="translate: none; rotate: none; scale: none; transform: translate(0%, -35%) scale(1.15, 1.15);">
            <div class="visual__meta">
              <strong>Hosted demonstration layer</strong>
              <span>Concierge-style presentations connect visible stadium data to SAP's product ecosystem.</span>
            </div>
          </article>
        </div>
      </div>
    </section>

    <section class="section">
      <div class="section__inner">
        <div class="grid grid--2">
          <article class="panel">
            <p class="eyebrow">Enterprise integration</p>
            <h2>The room should close the gap between entertainment and business.</h2>
            <ul class="list">
              <li>Invite current clients, target accounts, and regional prospects into a space that feels premium without feeling generic.</li>
              <li>Use the room to host guided product moments before kickoff, during halftime, and after the game.</li>
              <li>Package stadium tours with suite access so the venue becomes part of the executive hospitality story.</li>
              <li>Integrate SAP technology throughout the stadium through analytics touchpoints, premium-device experiences, and synchronized data surfaces so the suite connects to a larger venue story.</li>
              <li>Layer in keynote nights and app intelligence to turn one game day touchpoint into a broader partnership system.</li>
            </ul>
          </article>

          <article class="panel">
            <p class="eyebrow">Measures of success</p>
            <h2>What Baylor should promise to track</h2>
            <ul class="list">
              <li>Identify every premium guest through RSVP lists, hosted seating plans, check-in logs, and post-event follow-up ownership.</li>
              <li>Track hosted guest count and mix by client, prospect, donor, or strategic partner category along with executive meetings scheduled after each event.</li>
              <li>Measure suite interaction rate across the table, per-seat devices, hosted demos, and stadium-wide SAP touchpoints seen by premium guests.</li>
              <li>Connect each hosted game to follow-up activity, opportunity creation, and account movement so the suite is measured as business development, not soft hospitality spend.</li>
              <li>Use immediate post-event surveys plus mid-season and post-season brand-lift surveys to see whether SAP is increasingly viewed as useful, innovative, and worth doing business with.</li>
            </ul>
          </article>
        </div>
      </div>
    </section>

    <section class="section">
      <div class="section__inner">
        <div class="grid grid--2">
          <article class="panel">
            <p class="eyebrow">Targeting and education</p>
            <h2>Why this reaches the right audience for SAP</h2>
            <ul class="list">
              <li>Priority invitees should include current SAP clients, target accounts along the I-35 corridor, Baylor donors with enterprise influence, and regional operators in manufacturing, healthcare, logistics, and public institutions.</li>
              <li>The experience actively teaches fans and guests what SAP does by translating live data, operations logic, and decision support into something visible and useful inside the room.</li>
              <li>This is a direct B2B and B2C crossover: premium guests see the product demonstration while the surrounding game-day context keeps the environment emotionally memorable.</li>
              <li>Baylor acts as the guide by giving SAP a controlled place to host, explain, and follow up instead of relying on passive signage alone.</li>
              <li>The prestige factor matters too: a high-caliber suite gives decision-makers a reason to attend, enjoy the environment, and associate SAP with premium access and serious business hospitality.</li>
            </ul>
          </article>

          <article class="panel">
            <p class="eyebrow">Asset mix and pricing</p>
            <h2>How this activation satisfies the execution rubric</h2>
            <ul class="list">
              <li>Minimum communication assets: invitation email series, suite entry branding, center-table analytics takeover, per-seat iPad interface, host demo script, and post-event executive recap.</li>
              <li>Recommended annual allocation inside the broader integration package: roughly $70K-$120K depending on number of hosted games, device count, and concierge staffing.</li>
              <li>The idea is not generic or easily transferable because the value comes from SAP visibly running the intelligence layer of the room, not simply putting a logo on hospitality inventory.</li>
              <li>Optional premium add-ons can include a stadium innovation tour stop, a branded concierge host, and account-specific post-event recaps for SAP sales follow-up.</li>
              <li>Creatively, this is where sponsor, property, and fan overlap best: Baylor delivers access, SAP delivers intelligence, and guests leave with both emotional memory and business relevance.</li>
            </ul>
          </article>
        </div>
      </div>
    </section>
  </main>

  <nav class="nav-island" aria-label="Primary navigation">
    <a href="index.html">Overview</a>
    <a href="sap-jersey-patch.html">Patch</a>
    <a class="is-active" href="sap-suites.html">Suites</a>
    <a href="sap-keynote.html">Keynotes</a>
    <a href="packages.html">App</a>
    <a href="contact.html">Contact</a>
  </nav>

  <script src="https://cdnjs.cloudflare.com/ajax/libs/gsap/3.12.5/gsap.min.js"></script>
  <script src="https://cdnjs.cloudflare.com/ajax/libs/gsap/3.12.5/ScrollTrigger.min.js"></script>
  <script>
    document.addEventListener("DOMContentLoaded", () => {
      const transitionWrap = document.querySelector(".page-transition");
      const panel = transitionWrap?.querySelector(".page-transition__panel");
      const hasGSAP = typeof gsap !== "undefined";
      const hasScrollTrigger = typeof ScrollTrigger !== "undefined";
      let parallaxInitialized = false;
      let isLeaving = false;

      if (!transitionWrap || !panel) return;

      const pageNodes = [
        document.querySelector("main"),
        document.querySelector(".nav-island")
      ].filter(Boolean);

      const setCompactPanel = () => {
        panel.style.opacity = "0";
        panel.style.left = "50%";
        panel.style.top = "50%";
        panel.style.width = "min(34vw, 460px)";
        panel.style.height = "min(22vw, 300px)";
        panel.style.borderRadius = "28px";
        panel.style.transform = "translate(-50%, -50%) scale(0.88)";
      };

      const cleanupPageNodes = () => {
        pageNodes.forEach((node) => {
          node.style.removeProperty("opacity");
          node.style.removeProperty("transform");
          node.style.removeProperty("will-change");
        });
      };

      const primePanelFromElement = (element) => {
        const rect = element?.getBoundingClientRect?.();
        if (!rect || !rect.width || !rect.height) {
          gsap.set(panel, {
            opacity: 1,
            left: "50%",
            top: "50%",
            width: "min(34vw, 460px)",
            height: "min(22vw, 300px)",
            borderRadius: 28,
            scale: 0.88,
            xPercent: -50,
            yPercent: -50
          });
          return;
        }

        gsap.set(panel, {
          opacity: 1,
          left: rect.left + rect.width / 2,
          top: rect.top + rect.height / 2,
          width: Math.max(rect.width, 92),
          height: Math.max(rect.height, 44),
          borderRadius: Math.min(Math.max(rect.height * 0.5, 18), 28),
          scale: 1,
          xPercent: -50,
          yPercent: -50
        });
      };

      const initParallax = () => {
        if (!hasGSAP || !hasScrollTrigger) return;

        gsap.registerPlugin(ScrollTrigger);

        if (!parallaxInitialized) {
          const heroImg = document.querySelector(".hero__media img");
          if (heroImg) {
            gsap.to(heroImg, {
              yPercent: 30,
              ease: "none",
              scrollTrigger: {
                trigger: ".hero",
                start: "top top",
                end: "bottom top",
                scrub: true
              }
            });
          }

          gsap.utils.toArray(".visual").forEach((visual) => {
            const img = visual.querySelector("img");
            if (img) {
              gsap.fromTo(
                img,
                { yPercent: -35 },
                {
                  yPercent: 8,
                  ease: "none",
                  scrollTrigger: {
                    trigger: visual,
                    start: "top bottom",
                    end: "bottom top",
                    scrub: true
                  }
                }
              );
            }
          });

          parallaxInitialized = true;
        }

        ScrollTrigger.refresh();
      };

      const playEntrance = () => {
        document.body.classList.remove("is-transitioning");

        if (hasGSAP) {
          gsap.set(panel, {
            opacity: 1,
            left: "50%",
            top: "50%",
            width: "100vw",
            height: "100vh",
            borderRadius: 0,
            scale: 1,
            xPercent: -50,
            yPercent: -50
          });

          gsap.set(pageNodes, {
            opacity: 0,
            scale: 0.985,
            y: 18,
            transformOrigin: "50% 50%",
            willChange: "transform, opacity"
          });

          const tl = gsap.timeline({
            onComplete: () => {
              transitionWrap.classList.remove("is-entering", "is-leaving");
              setCompactPanel();
              cleanupPageNodes();
              initParallax();
            }
          });

          tl.to(panel, {
            opacity: 0,
            duration: 0.38,
            ease: "power2.out"
          }, 0).to(pageNodes, {
            opacity: 1,
            scale: 1,
            y: 0,
            duration: 0.56,
            ease: "power3.out",
            stagger: 0.03
          }, 0.06);
        } else {
          setCompactPanel();
        }
      };

      const playExit = (targetURL, triggerElement) => {
        if (isLeaving) return;
        isLeaving = true;
        transitionWrap.classList.add("is-leaving");
        document.body.classList.add("is-transitioning");

        if (hasGSAP) {
          primePanelFromElement(triggerElement);

          const tl = gsap.timeline({
            onComplete: () => {
              window.location.href = targetURL;
            }
          });

          tl.to(pageNodes, {
            opacity: 0.24,
            scale: 0.992,
            y: -8,
            duration: 0.22,
            ease: "power2.out",
            stagger: 0.01
          }, 0).to(panel, {
            left: "50%",
            top: "50%",
            width: "100vw",
            height: "100vh",
            borderRadius: 0,
            scale: 1,
            duration: 0.44,
            ease: "power3.inOut"
          }, 0.05);
        } else {
          window.location.href = targetURL;
        }
      };

      const isInternalLink = (anchor) => {
        if (!anchor || !anchor.href) return false;
        if (anchor.target === "_blank") return false;
        if (anchor.hasAttribute("download")) return false;

        try {
          const url = new URL(anchor.href, window.location.origin);
          if (url.origin !== window.location.origin) return false;
          if (url.pathname === window.location.pathname && url.hash) return false;
          if (anchor.protocol === "javascript:" || anchor.protocol === "mailto:") return false;
          return true;
        } catch {
          return false;
        }
      };

      document.addEventListener("click", (event) => {
        const anchor = event.target.closest("a");
        if (!anchor || !isInternalLink(anchor)) return;

        try {
          const target = new URL(anchor.href, window.location.origin);
          if (target.pathname === window.location.pathname) return;
        } catch {
          return;
        }

        event.preventDefault();
        sessionStorage.setItem("__pt_pending", "1");
        playExit(anchor.href, anchor);
      });

      const shouldEnter = sessionStorage.getItem("__pt_pending") === "1";
      if (shouldEnter) {
        sessionStorage.removeItem("__pt_pending");
        playEntrance();
      } else {
        setCompactPanel();
        cleanupPageNodes();
        initParallax();
      }

      window.addEventListener("pageshow", (event) => {
        if (event.persisted) {
          transitionWrap.classList.remove("is-leaving");
          setCompactPanel();
          cleanupPageNodes();
          initParallax();
        }
      });
    });
  </script>






</body></html>
